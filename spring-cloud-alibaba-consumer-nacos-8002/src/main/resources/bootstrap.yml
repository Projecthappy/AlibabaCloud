spring:
  application:
    name: spring-cloud-alibaba-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:1111
      config:
        server-addr: 127.0.0.1:1111 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        namespace: 01f35de1-e593-4538-be55-0ad511882876
feign:
  sentinel:
    enabled: true